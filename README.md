# OrangeOS v1.0

> 一个基于 x86 架构，从 BootLoader 开始自主实现的教学型操作系统\
> Inspired by 《Orange'S：一个操作系统的实现》

------------------------------------------------------------------------

# 1. 项目简介

OrangeOS 是一个从零实现的小型操作系统项目。

项目目标：

-   理解计算机启动流程
-   掌握 BootLoader、Kernel、Interrupt、Process 等操作系统核心机制
-   实现一个具有 Shell、文件系统、多任务能力的微型 OS
-   在基础 OS 功能之上扩展具有展示性的特色模块

最终系统目标：

    BIOS
     |
    Boot Sector
     |
    Boot Loader
     |
    Kernel
     |
    User Interface

------------------------------------------------------------------------

# 2. 功能规划

项目功能分为：

-   核心系统模块（必须完成）
-   系统功能模块（重点完成）
-   创新扩展模块（根据进度完成）

------------------------------------------------------------------------

# 3. 核心系统模块

## 3.1 BootLoader 模块

目标：

实现：

    BIOS
     ↓
    Boot Sector
     ↓
    Loader
     ↓
    Kernel

主要功能：

-   BIOS启动
-   Boot Sector加载
-   Loader加载Kernel
-   初始化运行环境
-   跳转Kernel入口

启动效果：

    ====================
         OrangeOS
    ====================

    Loading Kernel...

------------------------------------------------------------------------

## 3.2 Kernel 内核模块

负责：

-   系统初始化
-   硬件资源管理
-   提供系统服务

结构：

    kernel/

    ├── main.c
    └── kernel.asm

启动：

    OrangeOS Kernel Started

------------------------------------------------------------------------

## 3.3 保护模式模块

实现：

-   GDT
-   IDT
-   CPU模式切换
-   32位运行环境

展示：

    CPU Mode:

    Protected Mode
    32-bit

------------------------------------------------------------------------

## 3.4 中断管理模块

目录：

    interrupt/

    ├── idt.c
    ├── clock.c
    └── keyboard.c

实现：

### 时钟中断

功能：

-   系统时间
-   任务调度

### 键盘中断

功能：

-   用户输入
-   Shell交互

------------------------------------------------------------------------

## 3.5 进程管理模块

实现 PCB：

``` c
PROCESS
{
    PID;
    State;
    Registers;
    Stack;
}
```

支持：

    Task A
    Task B
    Task C

调度：

    Running
       ↓
     Ready
       ↓
    Running

计划实现：

-   时间片轮转调度
-   任务状态管理
-   进程列表显示

------------------------------------------------------------------------

# 4. 系统功能模块

## 4.1 Shell 命令系统

启动：

    OrangeOS Shell
    >

基础命令：

  命令    功能
  ------- ----------
  help    查看帮助
  clear   清屏
  info    系统信息
  task    查看任务
  mem     查看内存
  ls      文件列表
  cat     查看文件

扩展：

-   命令历史
-   参数解析
-   命令补全（预留）

------------------------------------------------------------------------

## 4.2 简易文件系统

目标：

实现 OrangeFS 简易文件系统。

结构：

    /
    |
    ├── hello.txt
    ├── about.txt
    └── config.txt

支持：

    ls

    cat hello.txt

    write file.txt

预留：

-   文件创建
-   文件删除
-   文件属性管理

------------------------------------------------------------------------

# 5. 内存管理模块

目录：

    memory/

阶段目标：

## 基础阶段

实现：

-   内存检测
-   内存统计

显示：

    Memory:

    640KB Available

## 扩展阶段

实现：

-   动态内存分配
-   malloc/free
-   内存块管理

## 冲刺阶段

实现：

-   分页机制
-   虚拟地址映射

------------------------------------------------------------------------

# 6. 创新扩展模块

## 6.1 Orange Monitor 系统监视器 ⭐

类似 Linux top / Windows任务管理器。

展示：

    +----------------------+
    | Orange Monitor       |
    +----------------------+

    CPU Usage:

    ██████----

    Memory:

    ████------

    Process:

    PID    STATE

    1      RUNNING
    2      READY

    +----------------------+

涉及：

-   进程管理
-   内存管理
-   系统状态监控

当前命令：

    monitor    进入每 0.1 秒刷新的实时监视界面，Q/Esc 返回
    status     输出单行状态快照

实时界面展示内存页统计、调度切换、当前 PID，以及每个任务的状态、累计运行 tick
和 CPU 占比。

------------------------------------------------------------------------

## 6.2 Memory Visualization 内存可视化 ⭐

显示：

    Memory Map


    0x00000

    [#####---------]

    Kernel


    [---###-------]

    Process


    [-----------]

    Free

展示：

-   内存布局
-   内存分配情况
-   页管理状态

当前 Shell 命令：

    memmap

输出受管物理页的已用/总数和 20 格占用条；配合 `alloc`、`dealloc` 可观察实时变化。

------------------------------------------------------------------------

## 6.3 图形化界面 GUI（预留）

如果项目进度允许，实现：

    +----------------+
    | Orange Desktop |
    +----------------+
    | Terminal       |
    | File Manager   |
    +----------------+

功能：

-   VGA图形模式
-   窗口绘制
-   鼠标支持

------------------------------------------------------------------------

## 6.4 Mini Game（预留）

用于系统展示。

候选：

-   贪吃蛇
-   迷宫游戏
-   简单图形小游戏

------------------------------------------------------------------------

## 6.5 图形化 Shell（预留）

将字符终端升级为：

    +----------------+
    | Terminal       |
    | > help         |
    | > task         |
    +----------------+

------------------------------------------------------------------------

# 7. 项目目录结构

    OrangeOS/

    ├── boot/
    │   ├── boot.asm
    │   └── boot.bin
    │
    ├── loader/
    │   └── loader.asm
    │
    ├── kernel/
    │   ├── main.c
    │   └── kernel.asm
    │
    ├── hal/
    │   ├── port.c
    │   └── device.c
    │
    ├── interrupt/
    │   ├── idt.c
    │   ├── clock.c
    │   └── keyboard.c
    │
    ├── process/
    │   ├── process.c
    │   └── scheduler.c
    │
    ├── memory/
    │   └── memory.c
    │
    ├── filesystem/
    │   └── fs.c
    │
    ├── shell/
    │   └── shell.c
    │
    ├── monitor/
    │   └── monitor.c
    │
    ├── gui/
    │
    ├── game/
    │
    ├── lib/
    │
    ├── include/
    │
    ├── Makefile
    └── README.md

------------------------------------------------------------------------

# 8. 开发计划

## Week 1：启动系统

目标：

    BIOS
     ↓
    BootLoader
     ↓
    Kernel

完成：

-   Boot Sector
-   Loader
-   Protected Mode
-   Kernel入口

------------------------------------------------------------------------

## Week 2：核心 OS

完成：

-   GDT
-   IDT
-   Clock Interrupt
-   Keyboard Interrupt
-   Process Scheduler

------------------------------------------------------------------------

## Week 3：系统功能

完成：

-   Shell
-   File System
-   Memory Management

------------------------------------------------------------------------

## Week 4：特色与优化

完成：

-   Orange Monitor
-   Memory Visualization
-   启动动画
-   GUI（可选）
-   Mini Game（可选）
-   项目文档与Demo

------------------------------------------------------------------------

# 9. 开发原则

1.  每个模块必须可运行验证

2.  每个阶段提交 Git：

例如：

    Day1: boot sector works

    Day5: boot loader completed

    Day10: protected mode enabled

3.  优先保证系统稳定，再扩展特色功能

4.  每次修改后执行自动启动回归：

        make test

    测试会通过 QEMU 调试端口确认 Kernel 已启动并进入 Shell，且不会写回磁盘镜像。

------------------------------------------------------------------------

# 10. 项目目标

最终 OrangeOS 希望达到：

-   能独立启动
-   拥有自己的 Kernel
-   支持多任务
-   支持文件管理
-   拥有交互 Shell
-   提供系统监控与可视化展示

------------------------------------------------------------------------

# 11. Ring 3 用户态

当前已建立内核 GDT、32 位 TSS、独立特权级中断栈，以及三个仅标记为用户可访问的
代码/数据/栈页面。Shell 命令：

    user

会进入 CPL3，通过 `int 0x80` 获取 ticks、空闲页和 PID，再通过受控 `SYS_EXIT`
返回 Shell。

验证：

    make test-ring3

独立用户进程命令：

    run       启动调度器管理的 PID 4 用户进程
    ps        查看 PID 4 状态和累计运行 tick
    kill 4    终止 PID 4

PID 4 在 Ring 3 运行约五秒后调用 `SYS_EXIT`，调度器将其标记为 `EXITED` 并恢复其他任务。
自动生命周期验证：

    make test-process

用户态异常隔离：

    runfault   让 PID 4 在 Ring 3 访问未映射页面
    ps         查看 FAULTED 状态、CR2 和页故障错误码

Ring 3 页故障只终止 PID 4；Ring 0 页故障仍会保护性停机。自动验证：

    make test-userfault

用户进程的代码、数据和栈页现在按运行动态分配，并在正常退出、页故障或 `kill 4`
时解除映射和归还。验证运行中增加三页、退出后恢复基线：

    make test-resources

OrangeFS 用户程序采用 `OEX2:EE:LL:CC:<hex machine code>` 格式，其中包含入口偏移、
代码长度和 8 位校验和。可通过以下命令加载到
动态用户代码页并作为 PID 4 执行：

    exec demo.oex
    ps

默认 `demo.oex` 设置退出结果 42，`ps` 应显示 `code=42`。自动验证：

    make test-exec

系统调用 `SYS_WRITE`（EAX=5）允许 Ring 3 程序以 `ESI=用户地址`、`ECX=长度`
请求内核输出文本。内核验证调用者特权级、长度和地址范围，只接受当前映射的用户代码、
数据或栈页，并先复制到最长 127 字节的内核缓冲区。默认 `demo.oex` 会输出：

    Hello from Ring3 OEX!

随后设置退出码 42 并通过 `SYS_EXIT` 结束。这证明磁盘中的用户程序不能直接操作 VGA，
而是通过受控 ABI 使用内核服务。

损坏格式验证：

    exec bad.oex

应返回 `Invalid OEX2 executable`，且不会启动用户进程。

------------------------------------------------------------------------

# 12. OrangeFS v3

ORF3 将目录与数据分离：LBA 42 保存目录，后续为 7 个双扇区文件 extent。
单文件容量由 63 字符提升到 1023 字节，并新增：

    stat <file>

跨扇区默认文件：

    stat big.txt

当前镜像布局下预期显示 `size=700 bytes start=88 sectors=2`。自动验证同时检查偏移 511/512
两侧的数据：

    make test-fs-large

------------------------------------------------------------------------

# 13. OrangeFS v4 位图分配

ORF4 在目录中加入 32 位数据扇区位图。`touch` 动态分配两个连续扇区，`rm`
归还位图，后续文件会复用已释放的 extent。

    disk
    touch temp
    disk
    rm temp
    disk

空闲扇区应呈现 `20 -> 18 -> 20`。自动验证：

    make test-fs-bitmap

------------------------------------------------------------------------

# 14. OrangeFS v5 可变 extent

ORF5 按实际内容分配空间：`touch` 只建立目录项，不占数据扇区；写入不超过
512 字节时分配 1 个扇区，超过 512 字节时分配 2 个连续扇区。文件长度跨过
512 字节边界时会重新分配 extent，`rm` 会按实际扇区数归还位图。

默认 6 个文件共使用 7 个扇区，因此启动后 `disk` 显示 `free=25/32`。

    disk
    touch temp
    disk
    write temp hi
    disk
    rm temp
    disk

预期空闲扇区变化为 `25 -> 25 -> 24 -> 25`。自动验证：

    make test-fs-large
    make test-fs-bitmap

------------------------------------------------------------------------

# 15. OrangeFS v6 目录校验

ORF6 在目录扇区末尾保存 XOR 校验值。每次创建、写入或删除文件后，系统会先
重新计算校验值，再把完整目录扇区写入磁盘。启动时同时验证 `ORF6` 魔数和整个
目录扇区；如果元数据被破坏，当前版本会重新建立默认文件系统，避免继续使用
错误的位图或 extent。

故障注入测试会复制磁盘镜像、篡改 LBA 42 的位图字节，然后重新启动验证恢复：

    make test-fs-checksum

------------------------------------------------------------------------

# 16. OrangeFS v7 双目录恢复

ORF7 保留两份带校验和版本号的目录：主目录位于 LBA 42，备份目录位于
LBA 75。每次目录更新时版本号递增，并按“备份优先、主目录随后”的顺序写入。
启动时分别验证两份目录，选择校验有效且版本较新的副本，再同步修复另一份。

`make test-fs-checksum` 现在还会先创建并写入用户文件 `keep`，再从宿主机破坏
主目录。重启后必须仍能读出 `survives`，并保持 `free=24/32`，证明恢复没有
重新格式化或丢失用户目录项。

------------------------------------------------------------------------

# 17. OrangeFS v8 文件数据校验

ORF8 使用目录项最后两个字节保存文件内容的 16 位加法校验。默认文件格式化和
每次 `write` 后都会更新校验；`cat` 与 `exec` 读取数据后必须先通过校验，否则
返回 `File data corrupt`，损坏的 OEX 程序不会进入 Ring3。

故障注入验证：

    make test-fs-data-checksum

------------------------------------------------------------------------

# 18. 完整功能冲刺：补全与启动动画

为继续实现 README 中剩余的 GUI 和 Mini Game，内核装载区由 20 KiB 扩展到
32 KiB（64 个扇区）；在 GUI 文件管理器阶段进一步扩展为 40 KiB（80 个扇区）。
当前 OrangeFS 布局为：主目录 LBA 82、数据区 LBA 83–114、备份目录 LBA 115。

Shell 支持唯一前缀 Tab 补全，例如输入 `hel` 后按 Tab 会补全为 `help`。验证：

    make test-autocomplete

Loader 启动动画已在下一阶段升级为视频帧播放，随后进入 32 位内核和 Shell。

------------------------------------------------------------------------

# 19. 自定义视频启动画面

启动画面取自用户提供视频的前 4 秒。构建资源 `assets/boot-video.bin` 包含 40 帧
320×200、16 色 VGA Mode 13h 数据，以 10 FPS 在 Loader 实模式阶段播放。播放完成后 BIOS
恢复 80×25 文本模式，再进入保护模式和 OrangeOS Shell。

视频帧从镜像 LBA 128 开始，不与内核或 OrangeFS 重叠。转换脚本：

    tools/video_to_vga.py <input.mp4> assets/boot-video.bin --seconds 4 --fps 10

当前实现播放画面但不播放视频音轨。

------------------------------------------------------------------------

# 20. GUI 桌面与 Mini Game

Shell 新增两个全屏应用：

    gui
    game
    tetris

`gui` 打开 VGA 彩色桌面，按 `Q` 或 `Esc` 返回 Shell。桌面分为 SYSTEM、FILES、
APPS 三个区域，用于答辩时展示系统能力入口。

`game` 启动 ORANGE CATCH：使用左右方向键移动玩家 `^`，接住下落的 `*` 得分，
按 `Q` 或 `Esc` 返回。游戏刷新由 PIT ticks 驱动，键盘由 IRQ1 实时处理。

`tetris` 启动 ORANGE TETRIS：包含 I/O/T/S/Z/J/L 七类四格方块和四种旋转状态，
左右方向键移动、上键旋转、下键软降、空格硬降。落地后方块写入 10×18 棋盘，完整行
会被消除并累计分数；堆叠到顶部时显示 Game Over，按 Q 或 Esc 返回 Shell。

自动验证：

    make test-ui
    make test-tetris

------------------------------------------------------------------------

# 21. 交互式终端与可操作桌面

Shell 终端新增：

- VGA 硬件光标，始终显示下一字符写入位置；
- 命令缓冲区由 64 扩展到 128 字符；
- 长命令到达第 80 列后自动换到下一行；
- `PageUp` 回看最近 16 条被卷走的屏幕行；
- `PageDown` 恢复实时终端；
- 回看模式右侧显示滚动条和 `^` 位置标记。

GUI 桌面现在支持上下方向键选择和 Enter 打开：SYSTEM 启动 Monitor，FILES 显示
OrangeFS 文件列表，APPS 启动 ORANGE CATCH。全屏应用返回后会重新恢复桌面焦点。

FILES 已升级为交互式文件管理器：上下键选择文件，Enter 打开内置文本编辑器；编辑器
支持普通字符、Shift、退格和回车，按 `F2` 将内容写回 OrangeFS，按 `Esc` 返回文件列表。
保存后的内容可由 Shell 的 `cat` 读取，并在非快照运行中跨重启保留。

文件管理器还支持完整 CRUD：按 `N` 输入文件名并新建，保存内容后按 `Delete` 进入
删除确认页，按 Enter 确认或 Esc 取消。删除会同步更新双目录并归还文件占用的 extent。

自动验证：

    make test-terminal
    make test-ui
    make test-ui-files
    make test-ui-crud

------------------------------------------------------------------------

# 22. 最终发布与答辩准备

完整发布回归统一使用：

    make test-release

该目标顺序执行全部 16 项 QEMU 自动测试，只有最后出现
`OrangeOS release regression PASSED (16/16)` 才视为候选发布版。

答辩前的人工验收步骤见 `docs/RELEASE_CHECKLIST.md`，按模块阅读源码的七天路线见
`docs/CODE_READING.md`。

成为一个具有完整结构和展示价值的小型操作系统。
